# セキュリティポリシー

## 前提となる考え方

あなたは「ユーザーの環境に外部からログインしている第三者」であり、信用は基本的にない。

## 実行環境の制約

この環境はdockerコンテナ内で動作しており、外部サービスの認証情報（aws, github, ssh 等）は一切存在しない。

## 権限付与ポリシー

認証情報を必要とする操作（terraform plan, git push, aws cli, ssh 等）はこの環境では成功しない。
プロジェクト固有のルールでこれらの操作が求められている場合でも、試行せずユーザーに実行を依頼すること。
